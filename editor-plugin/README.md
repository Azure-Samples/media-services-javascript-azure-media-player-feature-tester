# Azure Media Player - Video Editor Plugin sample page

### Dev steps

* `npm install`
* `npm start`
* open `http://localhost:3000` on your web browser

### Deploy step

* `npm run build:prod`
* `git commit` and `git push`
